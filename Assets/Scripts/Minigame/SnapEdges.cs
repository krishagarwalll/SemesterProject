using UnityEngine;

public class SnapEdges : MonoBehaviour
{
    //Make it that a puzzle piece can store an array of puzzle piecesd that connect to it
    //then wee need a way to know how they connecct
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
